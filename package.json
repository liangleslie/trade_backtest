{
  "name": "backtest",
  "version": "1.0.0",
  "description": "backtest",
  "scripts": {
    "start": "http-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
